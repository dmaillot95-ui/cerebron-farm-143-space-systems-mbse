import json,hashlib,pathlib,platform
items=[("bus",500,.15),("payload",250,.2),("propulsion",100,.25),("avionics",55,.2)];cbe=sum(x[1] for x in items);with_margin=sum(x[1]*(1+x[2]) for x in items);margin=with_margin-cbe;pct=100*margin/cbe;ok=cbe==905 and 1070<with_margin<1080 and 18<pct<20
out={"farm":143,"engine":"python-mbse-reference-suite-v2","test":"MASS_MARGIN_ROLLUP","cbe_mass_kg":cbe,"mass_with_margin_kg":with_margin,"aggregate_margin_kg":margin,"aggregate_margin_percent":pct,"status":"REFERENCE_SUITE_OK" if ok else "FAIL","scope":"MASS_BUDGET_REFERENCE_NOT_FULL_MBSE_TRACEABILITY_OR_DESIGN_VALIDATION","python":platform.python_version()};raw=json.dumps(out,sort_keys=True).encode();out["result_sha256"]=hashlib.sha256(raw).hexdigest();pathlib.Path("artifacts").mkdir(exist_ok=True);pathlib.Path("artifacts/f143_reference_suite.json").write_text(json.dumps(out,indent=2)+"\n");print(json.dumps(out));raise SystemExit(0 if ok else 1)
